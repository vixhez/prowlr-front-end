const ResetButton = ({ resetApp }) => (

    <button
        type="button"
        className="btn btn-success btn-circle btn-xl"
        onClick={ resetApp }
    >
        Wanna swipe again?
    </button>


);

export default ResetButton;